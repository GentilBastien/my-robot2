import { Game } from '../states/game';
import { ResponseStateEvent } from '../events/response-state-event';

export function resolveResponseStateEvent(game: Game, responseStateEvent: ResponseStateEvent): void {
  return;
}
